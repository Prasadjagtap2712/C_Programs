
#include<stdio.h>
 void main() {
	 int age;
	 printf("Enter your age: ");
	 scanf("%d", &age);
	 if (age >= 18) {
		 printf("You are allowed to watch movie\n");
	 }else{
		 printf("You are not allowed to watch movie\n");
	 }
	 
 }

